name: Deploy to AWS EC2

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

env:
  AWS_REGION: ap-south-1
  EC2_INSTANCE_ID: i-0ef3eda6dd281400b
  REPO_URL: https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/web100Acress/CRM-100acress.git

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Checkout code
    - name: Checkout code
      uses: actions/checkout@v4

    # 2️⃣ Setup Node.js
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    # 3️⃣ Configure AWS credentials
    - name: Configure AWS
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    # 4️⃣ Create SSH key for EC2
    - name: Setup SSH key
      run: |
        echo "${{ secrets.EC2_SSH_KEY }}" > ec2_key.pem
        chmod 400 ec2_key.pem

    # 5️⃣ Get EC2 public IP
    - name: Get EC2 Public IP
      id: ec2
      run: |
        PUBLIC_IP=$(aws ec2 describe-instances --instance-ids ${{ env.EC2_INSTANCE_ID }} \
          --query 'Reservations[0].Instances[0].PublicIpAddress' --output text)
        echo "EC2_PUBLIC_IP=$PUBLIC_IP" >> $GITHUB_OUTPUT
        echo "EC2 IP: $PUBLIC_IP"

    # 6️⃣ Wait for SSH
    - name: Wait for SSH
      run: |
        for i in {1..30}; do
          if ssh -o StrictHostKeyChecking=no -o ConnectTimeout=5 -i ec2_key.pem ubuntu@${{ steps.ec2.outputs.EC2_PUBLIC_IP }} "echo SSH ready" &>/dev/null; then
            echo "SSH is ready!"
            break
          fi
          echo "Attempt $i/30: SSH not ready, waiting 10s..."
          sleep 10
        done

    # 7️⃣ Deploy application
    - name: Deploy application
      run: |
        cat > deploy.sh << 'EOF'
        #!/bin/bash
        set -e
        echo "🚀 Starting deployment..."

        PROJECT_DIR=/var/www/crm

        # Ensure swap exists (helps npm builds on small instances)
        if [ ! -f /swapfile ]; then
          echo "💾 Creating swapfile..."
          if ! sudo fallocate -l 2G /swapfile 2>/dev/null; then
            sudo dd if=/dev/zero of=/swapfile bs=1M count=2048
          fi
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile || true
        fi

        # Create project directory if missing
        [ ! -d "$PROJECT_DIR" ] && sudo mkdir -p "$PROJECT_DIR" && sudo chown ubuntu:ubuntu "$PROJECT_DIR"

        cd "$PROJECT_DIR"

        # Clone repo if not exists, else pull latest changes
        if [ ! -d ".git" ]; then
          echo "📥 Cloning repository..."
          git clone ${{ env.REPO_URL }} .
        else
          echo "📥 Pulling latest changes..."
          git pull origin main
        fi

        # Frontend build
        echo "📦 Frontend install & build..."
        cd acre-flow-crm
        npm install
        npm run build

        # Backend install
        echo "📦 Backend install..."
        cd ../crm-backend
        npm install

        # PM2 restart/start
        echo "🔄 Restarting PM2..."
        cd "$PROJECT_DIR"
        pm2 install pm2-logrotate || true
        pm2 restart ecosystem.config.js || pm2 start ecosystem.config.js
        pm2 save

        # Restart Nginx
        echo "🌐 Restarting Nginx..."
        sudo systemctl restart nginx

        echo "✅ Deployment completed!"
        EOF

        # Copy and execute script on EC2 (run in background with nohup)
        scp -o StrictHostKeyChecking=no -i ec2_key.pem deploy.sh ubuntu@${{ steps.ec2.outputs.EC2_PUBLIC_IP }}:/tmp/
        ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=30 -o ServerAliveCountMax=10 -i ec2_key.pem ubuntu@${{ steps.ec2.outputs.EC2_PUBLIC_IP }} "chmod +x /tmp/deploy.sh && nohup /tmp/deploy.sh > deploy.log 2>&1 &"

    # 8️⃣ Health check
    - name: Health check
      run: |
        echo "⏳ Waiting for services to start..."
        sleep 60

        echo "🏥 Checking frontend..."
        curl -f -s http://${{ steps.ec2.outputs.EC2_PUBLIC_IP }} || (echo "Frontend down" && exit 1)
        echo "✅ Frontend accessible"

        echo "🏥 Checking backend..."
        curl -f -s http://${{ steps.ec2.outputs.EC2_PUBLIC_IP }}/api || (echo "Backend down" && exit 1)
        echo "✅ Backend accessible"

    # 9️⃣ Cleanup
    - name: Cleanup
      run: rm -f ec2_key.pem deploy.sh

    # 🔟 Deployment summary
    - name: Deployment summary
      run: |
        echo "🎉 Deployment completed!"
        echo "🌐 Frontend: http://${{ steps.ec2.outputs.EC2_PUBLIC_IP }}"
        echo "🔧 Backend: http://${{ steps.ec2.outputs.EC2_PUBLIC_IP }}/api"
        echo "📊 PM2 Status: ssh -i ec2_key.pem ubuntu@${{ steps.ec2.outputs.EC2_PUBLIC_IP }} 'pm2 status'"
